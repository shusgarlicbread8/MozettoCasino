# WP-030 golden fixtures

Frozen TypeScript NLHE oracle vectors. Regenerated by:

```bash
pnpm --filter @mozetto/game-rules generate:fixtures
```

Verified by `src/freeze.test.ts` (fails on behavioral drift).

See `docs/WP-030_TS_ENGINE_FREEZE.md` and `manifest.json`.
