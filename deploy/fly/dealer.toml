# Fly.io recipe — confidential dealer HTTP service (prefer private networking).

app = 'mozetto-dealer-staging'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile.dealer'

[env]
  NODE_ENV = 'production'

[http_service]
  internal_port = 4003
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '15s'
    timeout = '5s'
    grace_period = '30s'
    method = 'GET'
    path = '/health'

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
