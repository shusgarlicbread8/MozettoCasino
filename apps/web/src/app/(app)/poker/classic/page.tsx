"use client";

import { ArenaFindMatch } from "@/components/ArenaFindMatch";

/** Poker (Classic) — multiway 6-max. */
export default function PokerClassicPage() {
  return <ArenaFindMatch product="classic" />;
}
