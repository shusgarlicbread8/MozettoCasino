"use client";

import { StrategySetup } from "@/components/strategy/StrategySetup";

/** WP-123 — Strategy / AI setup (Plan 20A). Replaces mock coaching / free-text prompts. */
export default function MyAiPage() {
  return <StrategySetup />;
}
