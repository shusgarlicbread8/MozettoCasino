"use client";

import { StrategySetup } from "@/components/strategy/StrategySetup";

/** Onboarding alias for WP-123 strategy setup. */
export default function MyAiSetupPage() {
  return <StrategySetup compact />;
}
