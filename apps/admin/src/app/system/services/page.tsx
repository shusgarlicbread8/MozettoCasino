import { ControlStubPage } from "../../../components/control/ControlStubPage";

export default function SystemServicesPage() {
  return (
    <ControlStubPage
      title="Services"
      description="API, game-server, agent-runtime, indexer, dealer heartbeats and versions."
      wave="Wave C3 (MC-032)"
    />
  );
}
