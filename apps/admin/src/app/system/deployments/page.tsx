import { ControlStubPage } from "../../../components/control/ControlStubPage";

export default function SystemDeploymentsPage() {
  return (
    <ControlStubPage
      title="Deployments"
      description="Release baseline: commit, env, migration head, feature flags."
      wave="Wave C3 / C11"
    />
  );
}
