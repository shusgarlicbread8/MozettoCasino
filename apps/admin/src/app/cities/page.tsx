import { ControlStubPage } from "../../components/control/ControlStubPage";

export default function CitiesPage() {
  return (
    <ControlStubPage
      title="Cities & Stakes"
      description="Per-city utilization, rake, and margin by stake ladder."
      wave="Wave C4 (MC-041–042)"
    />
  );
}
