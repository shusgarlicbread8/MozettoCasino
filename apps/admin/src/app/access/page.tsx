import { ControlStubPage } from "../../components/control/ControlStubPage";

export default function AccessPage() {
  return (
    <ControlStubPage
      title="Access"
      description="admin_principals, roles, revocation. Wallet allowlist via ADMIN_SUPERADMIN_ADDRESSES."
      wave="Wave C9 (MC-095)"
    />
  );
}
