import { ControlStubPage } from "../../components/control/ControlStubPage";

export default function RiskPage() {
  return (
    <ControlStubPage
      title="Risk & Integrity"
      description="Pairing, rat-hole, linked accounts, collusion signals. Restrict new matchmaking only."
      wave="Wave C5 (MC-050–051)"
    />
  );
}
