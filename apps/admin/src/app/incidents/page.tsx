import { ControlStubPage } from "../../components/control/ControlStubPage";

export default function IncidentsPage() {
  return (
    <ControlStubPage
      title="Incidents"
      description="SEV board, timelines, runbooks, linked audit actions."
      wave="Wave C10 (MC-100–103)"
    />
  );
}
