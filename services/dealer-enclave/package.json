{
  "name": "@mozetto/dealer-enclave",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "WP-054 Nitro Enclave dealer scaffold — mock attestation for Anvil; real Nitro requires AWS",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "tsx watch --env-file=../../.env.local src/server.ts",
    "start": "tsx src/server.ts",
    "start:local": "tsx --env-file=../../.env.local src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@mozetto/dealer-deck": "workspace:*",
    "@mozetto/protocol-vectors": "workspace:*",
    "fastify": "^5.2.1",
    "viem": "^2.55.11",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
